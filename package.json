{
  "name": "automatizados-web",
  "version": "1.0.0",
  "description": "automatizados",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "open": "cypress open",
    "open:devMarcos": "cypress open --config baseUrl=http://localhost:5173 --env AMBIENTE=DEV,USUARIO=MARCOS",
    "open:devFelipe": "cypress open --config baseUrl=http://localhost:5173 --env AMBIENTE=DEV,USUARIO=FELIPE",
    "open:devRodrigo": "cypress open --config baseUrl=http://localhost:5173 --env AMBIENTE=DEV,USUARIO=RODRIGO"
  },
  "author": "marcos",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.9.0"
  }
}
